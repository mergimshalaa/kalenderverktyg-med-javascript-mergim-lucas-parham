/**
 * Opens a popup window upon click.
 */
export function openAddEventWindow() {
    const addEventWindow = document.querySelector(".addEventWindow");
    addEventWindow.classList.toggle("displayFlex")
}